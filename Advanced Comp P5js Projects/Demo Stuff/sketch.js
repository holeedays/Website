var font;

function preload() {
  font = loadFont("./SodaCream.otf"); // not sure if this even works
}

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
}

class textAnim {

  constructor(xPos, yPos, string) {

    this.x = xPos;

    this.y = yPos;

    this.array = ;
  }



}